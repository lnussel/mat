- listen to signals to update list
- key to clone and rename images
- dispatch events to current dialog
- dialog for import => upsteam systemd needs polkit actions
- user name for shell
- edit feature (requires our own dbus service)
